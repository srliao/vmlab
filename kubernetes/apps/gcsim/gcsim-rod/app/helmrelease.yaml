---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/common-3.0.4/charts/library/common/values.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: &app gcsim-rod
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 3.0.4
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    controllers:
      gcsim-rod:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/go-rod/rod
              tag: latest@sha256:1d99341065d5a16af15fa2132c536414b20d05a6e5c212173f28eb5e2ac1a30e
            resources:
              requests:
                memory: 1Gi
              limits:
                memory: 2Gi
    service:
      app:
        controller: gcsim-rod
        ports:
          http:
            port: 7317

