## Attempts to restore the newly created PVC during its initial creation
---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/volsync.backube/replicationdestination_v1alpha1.json
apiVersion: volsync.backube/v1alpha1
kind: ReplicationDestination
metadata:
  name: volsync-test-nfs-bootstrap
spec:
  trigger:
    manual: restore-once-bootstrap
  restic:
    repository: volsync-test-nfs-volsync-minio
    copyMethod: Snapshot
    volumeSnapshotClassName: nfs-client
    storageClassName: nfs-client
    cacheStorageClassName: nfs-client
    cacheCapacity: 1Gi
    cacheAccessModes: ["ReadWriteOnce"]
    capacity: 1Gi
    accessModes: ["ReadWriteOnce"]
    moverSecurityContext:
      runAsUser: 568
      runAsGroup: 568
      fsGroup: 568
